types:
  float, str: input
  int: slider
  bool: switch

colors:
  sunset: "#ff9e1b"

styles:
  slider:
    color: sunset
    size: normal

  input:
    color: sunset

  textField:
    color: sunset

  switch:
    color: sunset
    size: normal

  tooltip:
    arrow: false
    enterTouchDelay: 0

  button:
    color: sunset
    style:
      root:
        color: "#fefefe"
