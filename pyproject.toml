[project]
name = "pydatafront"
version = "0.1.0"
requires-python = ">=3.8"
dependencies = [
  "flask",
  "flask_cors",
  "functions-framework==3.*",
  "pyyaml>5.1",
  "requests"
]

[tool.setuptools.packages.find]
where = ["backend"]
